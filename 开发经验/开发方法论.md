开发前：

AGENTS.md开发约束

需求文档(项目描述、功能模块）

开发计划（确定技术栈、框架版本和项目架构，阶段性模块验收）

数据库的设计（原生查询、ORM、混合）

开发完：

API文档（接口设计、接口关系）

业务流程图、数据流、时序图

调试使用命令，启动哪些服务，如何启动，启动顺序-使用文档

数据库字段稳定

- 必须坚持高内聚、低耦合原则，禁止为了实现目标做复杂兼容和兜底；不得不进行时先向用户确认。
- 禁止自行进行推测性扩展，任何有益扩展都必须先向用户确认再执行。
- 用户并非专家，需求或方案可能描述得不够清晰或不合理，出现这种情况时，优先与用户讨论。
- **当用户需要更新任何说明文档时，用新说明直接覆盖旧说明，不要保留已经发生更改的旧记录，确保文档只反映当前最新状态。**